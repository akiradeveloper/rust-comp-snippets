struct CoordCompression {

}
impl CoordCompression {
    fn new(xs: Vec<i64>, step: usize) -> CoordCompression {
        CoordCompression {

        }
    }
    fn compress(&self, x: i64) -> usize {

    }
}